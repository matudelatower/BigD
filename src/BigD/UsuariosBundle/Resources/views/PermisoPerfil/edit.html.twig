    {% extends 'ThemeBundle:Default:layout_base.html.twig' %}
{% set title = 'Edicion PermisoPerfil' %}
{% block title %}
{{ title }}
{% endblock %}

    {% block body -%}

    <h4 class="page-header">PermisoPerfil</h4>
    <div class="row">
        <div class="col-xs-12 col-sm-12">            
            <div class="box-header">
                <div class="box-name ui-draggable-handle">
                    <i class="fa fa-search"></i>
                    <span>Alta de PermisoPerfil</span>
                </div>
            </div>
            <div class="box-content">
                <h4 class="page-header">{{ title }}</h4>
                <form action="{{ path('permisos_perfil_update', { 'id': entity.id }) }}" role="form" method="post">
                    <div class="box-body">
                        {{ form_widget(form) }}
                    </div>
                    <div class="form-group">
                        <div class="col-sm-9 col-sm-offset-3">
                            <button class="btn btn-primary" type="submit"><i class="fa fa-save"></i> Guardar</button>

                            <a class="btn btn-success" href="{{ path('permisos_perfil') }}">
                                Volver
                            </a>

                        </div>
                    </div>
                </form>


            </div>
        </div>
    </div>


            {% endblock %}


