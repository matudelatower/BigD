{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pais</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ entity.descripcion }}</td>
            </tr>
            <tr>
                <th>Codigopais</th>
                <td>{{ entity.codigoPais }}</td>
            </tr>
            <tr>
                <th>Codigoarea</th>
                <td>{{ entity.codigoArea }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('pais') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('pais_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
