{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pais list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>Codigopais</th>
                <th>Codigoarea</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('pais_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.descripcion }}</td>
                <td>{{ entity.codigoPais }}</td>
                <td>{{ entity.codigoArea }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('pais_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('pais_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('pais_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
